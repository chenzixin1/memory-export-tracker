{
  "name": "korea-memory-export-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "fetch": "node server/jobs/refreshTradeData.js",
    "validate:provisional": "node server/jobs/validateProvisionalDetail.js",
    "check": "node --check server/index.js && node --check server/jobs/refreshTradeData.js && node --check server/jobs/validateProvisionalDetail.js && node --check public/app.js && node --check workers/memory-export-worker.js && npm run validate:provisional",
    "deploy:cf": "wrangler deploy -c wrangler.memory-export.toml",
    "deploy:pages": "wrangler pages deploy public --project-name memory-export-tracker --branch main"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.13.2",
    "zod": "^4.4.3"
  }
}
